#include <stdio.h>
int main()
{
   float celsius, fahrenheit=0;
   printf("Input the temperature in °C : ");
   scanf("%f", &celsius);
   fahrenheit=(1.8*celsius)+32;
   printf("The temperature in °F : %f", fahrenheit);
   return 0;
}
